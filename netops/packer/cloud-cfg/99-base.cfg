manage_etc_hosts: true
disable_root: true
ssh_pwauth: false

system_info:
  distro: debian
  default_user:
    name: debian
    lock_passwd: true
    shell: /bin/bash
    groups: [adm, sudo]
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]
